# M5-SET-01 设备名称编辑页设计

> 日期：2026-07-26  
> 状态：已批准  
> 对齐：PRD-03 §8.5 / PRD-04 §18

## 行为

- 设置页点击「设备名称」→ `/settings/device-name`
- 输入框自动聚焦；去首尾空格；1～32 字符；禁止控制字符
- 「保存」仅在合法且有变化时可用；成功后落盘并返回
- 发现广播继续读取最新 `AppSettings.deviceName`（无需改引擎）

## 非目标

主题/语言独立页、自动接收、非法字符细分类（统一为「不支持的字符」）

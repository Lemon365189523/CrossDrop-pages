# M4-WIN-02 在资源管理器中显示 设计

> 日期：2026-08-03  
> 状态：已实现（能力原由 M4-MAC-02 覆盖；本补丁收口）  
> 对齐：PRD-04 桌面「在文件夹中显示」

## 目标

1. 确认 Windows `explorer /select,` + UI 入口已可用。  
2. Windows 文案改为「在资源管理器中显示」。  
3. `explorer` 成功时非零 exit 不误报失败。

## 非目标

重做打开文件逻辑、移动端分享。

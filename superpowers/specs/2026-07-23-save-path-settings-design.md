# M3-FILE-01 保存路径设置设计

> 日期：2026-07-23  
> 状态：已批准  
> 范围：设置页内联保存位置、选目录、可写校验、恢复默认

## 行为

- 展示当前 `savePath`，空则显示默认 Downloads/文档路径
- 选择目录后探测可写（创建并删除临时文件）；失败不落盘
- 「恢复默认」清空 `savePath`
- 接收引擎继续使用现有 `_resolveSaveDirectory`

## 非目标

独立子页、SAF 书签、精确剩余空间
